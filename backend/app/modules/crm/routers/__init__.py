"""Routers del módulo `crm` (skeleton F0). En F1 este archivo se vuelve el
agregador (`APIRouter(prefix="/crm")`) que incluye los sub-routers (person,
contact_identifier, lead_status, customer_status, lead_lifecycle, assignment,
activity); recién ahí se registra una vez en `app/main.py`.
Ver `docs/modules/crm/backend.md`.
"""
