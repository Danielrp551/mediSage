"""Services del módulo `crm` (skeleton F0). Módulos de funciones (no clases) que
reciben `AsyncSession`, lanzan excepciones de dominio (mensajes en español, `code`
en inglés) y devuelven los envelopes del template. Ver `docs/modules/crm/backend.md`.
"""
