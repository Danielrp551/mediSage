/**
 * Stub vacio para los modulos `server-only` y `client-only` en el entorno de pruebas
 * (jsdom). En produccion estos paquetes lanzan si se importan del lado equivocado; en
 * pruebas no aplican y se reemplazan por este modulo inerte via alias de Vitest.
 */
export {};
